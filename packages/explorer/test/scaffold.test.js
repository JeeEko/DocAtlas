import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('docatlas explorer scaffold', () => {
  it('placeholder passes', () => {
    assert.equal(1, 1);
  });
});
